import tw from "twin.macro";
import styled from "styled-components";

export const StyledCharacterListContainer = styled.div.attrs({
    className: "StyledCharacterListContainer"
})`
    ${tw`relative flex flex-row`}
`   